info:
  name: Get The Created New Booking
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/booking/{{BOOKING_ID}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
